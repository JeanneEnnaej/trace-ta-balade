require 'rails_helper'

RSpec.describe Step, type: :model do
  # it 'should persist a valid step' do
  #   step = build(:step)
  #   step.save
  #   expect(Step.count).to eq(1)
  # end

  # it 'delete step should decrement steps_count' do
  #   step = build(:step)
  #   step.save
  #   expect(Step.count).to eq(1)
  #   step.destroy
  #   expect(Step.count).to eq(0)
  # end
end
