require 'rails_helper'

RSpec.describe Walk, type: :model do
  # context 'step associations' do
  #   it 'should increment steps_count' do
  #     walk = build(:walk)
  #     walk.steps << build(:step)
  #     walk.save
  #     expect(Step.count).to eq(1)
  #     expect(walk.steps.count).to eq(1)
  #   end
  # end

  # it 'should persist a valid walk' do
  #   walk = build(:walk)
  #   walk.save
  #   expect(Walk.count).to eq(1)
  # end
end
